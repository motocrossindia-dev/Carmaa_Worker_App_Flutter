import 'package:flutter/material.dart';

class DailyDoorstepScreen extends StatelessWidget {
  const DailyDoorstepScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Daily Doorstep Screen'),
      ),
    );
  }
}
